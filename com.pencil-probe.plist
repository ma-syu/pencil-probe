<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- WHY launcher wrapper: plists are static XML with no variable
         support. The launcher reads ~/.config/pencil-probe.conf so
         users can change IP/port without modifying the plist. -->
    <key>Label</key>
    <string>com.pencil-probe</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/sh</string>
        <string>-c</string>
        <!-- ~/bin/ is not shell-expanded in plists, so $HOME is used.
             /bin/sh -c handles the expansion at runtime. -->
        <string>exec $HOME/bin/pencil-probe-launcher.sh</string>
    </array>

    <!-- WHY KeepAlive: pencil-probe runs as a long-lived daemon.
         If it crashes or the client disconnects, launchd restarts it
         automatically so it's ready for the next Pencil connection. -->
    <key>KeepAlive</key>
    <true/>

    <!-- WHY RunAtLoad: start automatically at login so users
         don't have to launch it manually each time. -->
    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/tmp/pencil-probe.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/pencil-probe.log</string>
</dict>
</plist>
